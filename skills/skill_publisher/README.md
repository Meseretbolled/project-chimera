# Skill: Publisher

## Purpose
Publish content only after validation + approval gates.

## Critical Governance
- Cannot run unless content is approved
- No direct API keys inside agent runtime
- Full trace emission required

Publisher is the final state-commit skill.
